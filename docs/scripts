### 脚本文件 说明
-
```
deal_mongo.py
    1. insert_2_xt: 
        单纯的插入一条数据
    2. update_info:
        数据格式{'name':'lion'
                'list': []}
        向字段 list 中添加数据
```

```
ipCheck.py
    1. ClassDBMongo: 
        将结果保存到 MongoDB 中
    2. ClassIPlace:
        - main : 从文件中获取待检测 IP 进行归属地检测
        - run : 从 MongoDB 中读取失败 IP 进行归属地检测
        - 此处用到了代理 IP ，未去验证是否百度做了 IP 限制
```